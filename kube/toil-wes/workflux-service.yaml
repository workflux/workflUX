apiVersion: v1
kind: Service
metadata:
  name: workflux
  labels:
    app: workflux
spec:
  type: NodePort
  selector:
    app: workflux
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30001